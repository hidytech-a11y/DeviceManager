@model DeviceManager.Models.Device

<div class="container mt-5">
    <div class="card shadow-lg border-2">
        <div class="card-header bg-gradient-info text-black-50 d-flex justify-content-between align-items-center">
            <h3 class="mb-0"><i class="bi bi-device-hdd"></i> Device Details</h3>
            <span class="badge bg-primary">@Model.Status</span>
        </div>
        <div class="card-body">
            <div class="mb-3">
                <strong>Name:</strong>
                <p class="text-muted">@Model.Name</p>
            </div>
            <div class="mb-3">
                <strong>Type:</strong>
                <p class="text-muted">@Model.Type</p>
            </div>
            <div class="mb-3">
                <strong>Status:</strong>
                <p class="text-muted">@Model.Status</p>
            </div>

            ```
            <div class="d-flex gap-2">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-primary">
                    <i class="bi bi-pencil-square"></i> Edit
                </a>
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Back
                </a>
            </div>
        </div>
    </div>
    

</div>
